<div id="yui-main">
    <div class="yui-b">
        <div class="yui-g accroche">
            <div id="boule-orange">
                <%= image_tag("Boule-orange.png") %>
            </div>
            <p>
                Je vous propose un panel de site modèles. Ces modèles couvrent les besoins de divers secteurs : associatif, immobilier, e-commerce et vous sont proposés à un tarif compétitif.
            </p>
        </div>
        
        <div class="yui-g">
            <h1>Nos sites modèles</h1>
            
            <% @modeles.each do |modele| %>
                <div class="modele">
                    <p class="image-modele"><%= modele.picture.url(:thumb) %></p>
                    <%= modele.desc %>
                    <p class="modele-link"><%= link_to modele.weburl, "#{modele.weburl}" %></p>
                </div>
            
            <% end %>

            <% if logged_in? %>
                <%= link_to 'Nouveau Modèle', new_modele_path %>
            <% end %>
        </div>
    </div>
</div>

